- Git repo with content in `content/` and briefs in `briefs/`.
- Style profile in `engine/styles/` (voice, H2 schema, CTA norms).
- Allowed domains set (or `no_external: true`).
- Optional: WordPress credentials in `.env` for draft publishing.

