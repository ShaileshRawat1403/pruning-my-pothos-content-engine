- Define rules (voice, H2s, allowlist) and add checklists.
- Seed notes in `content/`; re-index; set `RETRIEVE_MAX_PER_SOURCE=1`.
- Produce a deterministic draft; reviewers fix facts only.
- Lint, audit, preview; publish as draft; schedule.

