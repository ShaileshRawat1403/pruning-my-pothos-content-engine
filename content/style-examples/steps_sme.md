- Define rules: voice, H2 schema, allowed domains; add `must_include_terms`.
- Seed first‑party notes in `content/`; rebuild the index.
- Generate a deterministic draft; resolve gaps; re‑run.
- Lint and audit; preview; publish as draft; schedule.

