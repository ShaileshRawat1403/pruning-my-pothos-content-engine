TASK: Draft an Instagram caption summarizing the core idea with a practical angle.
TITLE: {title}
AUDIENCE: {audience}
GOAL: {goal}
TONE: {tone}
LENGTH: short
STYLE GUIDELINES:
{style}

STYLE EXAMPLES (imitate tone/structure, not wording):
{style_examples}

CONSTRAINTS:
- Ground every line in CONTEXT; if details are missing, highlight the gap instead of guessing.
{must_terms_line}
- Max 2200 characters total (hard limit).
- 1–2 short paragraphs. Start with a hook.
- Add one practical tip or example the reader can try.
- Use line breaks for readability. Avoid walls of text.
- Include 3–7 relevant hashtags at the end.

CONTEXT:
{context}

OUTPUT FORMAT:
- Plain text, no H1/H2.
- End with hashtags line: {hashtags}
