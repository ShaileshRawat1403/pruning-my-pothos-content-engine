TASK: Draft a {content_type}.
CONTROL TOKENS: {controls}
TITLE: {title}
AUDIENCE: {audience}
GOAL: {goal}
TONE: {tone}
LENGTH: {length}     # short | medium | long (use this to adjust depth and examples)
STYLE GUIDELINES:
{style}

STYLE EXAMPLES (imitate tone/structure, not wording):
{style_examples}

STRUCTURE CONSTRAINTS:
- Use only information grounded in CONTEXT; call out missing data instead of inventing.
{must_terms_line}
- H1 exactly once with the title
- H2: Overview, Why It Matters, Prerequisites, Steps, Examples, FAQs, References
- Use bullet points for steps; keep sentences short
- Cite sources inline like [1], [2], and list them under References with URLs

CITATION POLICY:
- Allowed domains (if provided): {allowed_domains}
- If allowed domains are provided, only cite URLs from this list. Do not include any other domains.
- If no allowed domains are provided but 'no_external' is true, do not include any external URLs. Use bracket citations without links or state "No external references available." under References.

CONTEXT:
{context}

SAFETY RAILS:
- Do not include product/model catalogs (e.g., random LLM model descriptions) unless directly relevant and present in CONTEXT.
- Do not output meta text like 'Document:', 'Your task:', apologies, or analysis instructions.
