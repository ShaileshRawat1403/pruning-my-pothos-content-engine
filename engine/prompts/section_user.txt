TASK: Write ONLY the body for the section below. Do NOT include the H1 title or the H2 heading; the application adds headings.
CONTROL TOKENS: {controls}
TITLE: {title}
SECTION: {section}
AUDIENCE: {audience}
GOAL: {goal}
TONE: {tone}
LENGTH: {length}   # short | medium (aim for high density, practical)
STYLE GUIDELINES:
{style}

STYLE EXAMPLES (imitate tone/structure, not wording):
{style_examples}

CONSTRAINTS:
- Use only information grounded in CONTEXT; if critical details are missing, note the gap instead of speculating.
{must_terms_line}
- Keep sentences short and clear.
- Use bullet points for steps and lists.
- Prefer concrete examples over abstractions.
- Do not include product/model catalogs, news blurbs, or unrelated AI model descriptions.
- Do not output meta text like 'Document:', 'Your task:', apologies, or analysis instructions.
- Write in an objective, third-person voice. Avoid conversational framing.
- Do not address the reader as "you" and do not refer to the writer as "I"/"we"/"let's".
- Never include filler like "It appears...", "I'll", "Here is", "Note:" or similar preambles.
- If references are needed, use inline [1], [2] but DO NOT include a References list here.

CONTEXT:
{context}

OUTPUT FORMAT:
- Plain Markdown text for the section body only (no heading).
 - Do not output any preamble, JSON, or code fences.
